apiVersion: v1
kind: Service
metadata:
  name: "{@ service_kebab @}-service"
  labels:
    app: "{@ service_kebab @}"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: "{@ service_kebab @}"
