# =============================================================================
# GCP Dev — ServiceAccount with Workload Identity
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{service}-sa"
  annotations:
    iam.gke.io/gcp-service-account: "{service}-sa-dev@{PROJECT_ID}.iam.gserviceaccount.com"
